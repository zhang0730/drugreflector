```
(base) cai026@ubuntu:~/Drugreflector$ conda create -n drugreflector python=3.8

(base) cai026@ubuntu:~/Drugreflector$ conda activate drugreflector

(drugreflector) cai026@ubuntu:~/Drugreflector$ pip install drugreflector
ERROR: Could not find a version that satisfies the requirement drugreflector (from versions: none)
ERROR: No matching distribution found for drugreflector
#好像不能自动下载 试试本地安装
(drugreflector) cai026@ubuntu:~/Drugreflector$ cd "/disk1/cai026/Drugreflector/drugreflector-main/"
(drugreflector) cai026@ubuntu:~/Drugreflector/drugreflector-main$ pip install .
```

这个会自动配置cuda torch  之后再看看它配置的对不对把

```
pip show torch numpy pandas scipy anndata scanpy
conda install ipykernel -y
python -m ipykernel install --user --name drugreflector --display-name drugreflector
```

